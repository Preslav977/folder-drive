<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/style.css">
  <title>Folder Drive Delete Folder</title>
</head>

<body>
  <% if (locals.subfolders) { %>
  <div class="dialog-container">
    <dialog id="delete-folder-modal">
      <p>Are you sure you want to delete this folder with all its content?</p>
      <form action="/folders/delete/<%= subfolders.id %>" method="POST">
        <button id="close-deletefolder-modal" type="submit" onclick="return confirm('Yes')">Delete</button>
      </form>
    </dialog>
  </div>
  <% } %>

</body>

</html>